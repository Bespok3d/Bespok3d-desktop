{
  "compilerOptions": {
    "composite":          true,
    "incremental":        true,
    "emitDeclarationOnly": true,
    "outDir":             "./node_modules/.cache/tsc/node",
    "tsBuildInfoFile":    "./node_modules/.cache/tsc/node.tsbuildinfo",
    "rootDir":            "..",
    "module":             "ESNext",
    "moduleResolution":   "bundler",
    "target":             "ES2022",
    "lib":                ["ES2022"],
    "strict":             true,
    "skipLibCheck":       true,
    "esModuleInterop":    true,
    "resolveJsonModule":  true,
    "types":              ["node"],
    "paths": {
      "@adapter-sdk":       ["./src/main/adapter-loader/index.ts"],
      "@adapters/*":        ["../adapters/*"],
      "@bespok3d/contract": ["../lib_bespok3d/ts/contract/index.ts"]
    }
  },
  "include": [
    "src/main/**/*",
    "src/main/**/*.json",
    "src/preload/**/*",
    "tests/**/*",
    "../lib_bespok3d/ts/contract/**/*",
    "../adapters/**/*",
    "electron.vite.config.*"
  ],
  "exclude": [
    "node_modules",
    "../adapters/**/*.test.ts",
    // The adapters repo vendors lib_bespok3d as a submodule so it can gate standalone. The app
    // already compiles the SDK from its own sibling above, so sweeping the vendored copy in would
    // compile the same contract twice and pull in that submodule's uninstalled devDependencies.
    "../adapters/lib_bespok3d/**",
    "../lib_bespok3d/ts/contract/**/*.test.ts"
  ]
}
